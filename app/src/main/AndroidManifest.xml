<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="strech.ina.lai.appstretch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activity.MainActivity"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.OnBoardingActivity"
            android:label="@string/title_activity_on_boarding"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.PostureListActivity" />
        <activity android:name=".activity.ActivityListActivity" />
        <activity android:name=".activity.PostureDetailActivity" />
        <activity android:name=".activity.ActivityDetailActivity" />
        <activity android:name=".activity.TipListActivity" />
        <activity android:name=".activity.TipDetailActivity" />
        <activity android:name=".activity.AdvancedCourseListActivity" />
        <activity android:name=".activity.ConfirmOrderActivity" />
        <activity
            android:name=".activity.SignInActivity"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>